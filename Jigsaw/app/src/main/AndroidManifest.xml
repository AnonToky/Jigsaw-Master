<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jigsaw"
        tools:targetApi="31">
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Jigsaw.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Jigsaw.FullScreen" />

        <activity
            android:name=".EditorActivity"
            android:exported="true"
            android:theme="@style/Theme.Jigsaw.FullScreen"
            android:screenOrientation="portrait">

        </activity>

        <activity
            android:name=".MyPuzzlesActivity"
            android:exported="false"
            android:theme="@style/Theme.Jigsaw.FullScreen"
            android:label="我的拼图" />

        <activity
            android:name=".LevelSelectionActivity"
            android:exported="true"
            android:theme="@style/Theme.Jigsaw.FullScreen" />

        <activity
            android:name=".GameActivity"
            android:exported="true"
            android:theme="@style/Theme.Jigsaw.FullScreen" />

        <activity android:name=".SettingsActivity"
            android:exported="false"
            android:label="设置" />

        <service
            android:name=".MusicService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:stopWithTask="false"
            tools:ignore="ForegroundServicePermission" />


        <activity
            android:name=".achievements.AchievementsActivity"
            android:exported="true"
            android:theme="@style/Theme.Jigsaw.FullScreen" />
    </application>

</manifest>